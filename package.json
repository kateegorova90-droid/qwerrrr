{
  "name": "wb-proxy",
  "version": "1.0.0",
  "description": "Прокси-сервер для Wildberries API — решает CORS для браузерных запросов",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
