{
  "name": "wb-proxy",
  "version": "3.0.0",
  "description": "Wildberries API proxy — zero npm dependencies",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
