services:
  - type: web
    name: wb-proxy
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /health
    envVars:
      - key: WB_TOKEN
        sync: false
      - key: PORT
        value: 10000
